/* 
 setTimeout()
 DOM Api's
 fetch()
 localStorage
 Console
 location


these are all the web api's and js just gives us access to use them on the js engine of the browser
using the window keyword that is.
  we can still use all of them without using the keyword windown if they are in the global scope
*/

//? The use of event loop is to put things inside the callback queue into the call stack.
  
//? tasks inside the microtask queue are of higher priority then the call back queue

//?all the callback funtion that come through promises will go inside microftask queue